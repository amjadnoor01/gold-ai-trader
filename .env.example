# GitHub Token (keep private — used for repo creation)
GITHUB_TOKEN=your_pat_here

# Paper trading config
INITIAL_BALANCE=10000.0
RISK_PER_TRADE_PCT=0.015
MAX_DAILY_DRAWDOWN_PCT=0.03

# Data symbols (all free via yfinance)
GOLD_SYMBOL=GC=F
MACRO_SYMBOLS=^TNX,DX-Y.NYB,^VIX,CL=F,^GSPC,BTC-USD

# Live feed
LIVE_POLL_SECONDS=10

# Paths
MODEL_PATH=train/ppo_xauusd_latest.zip

# Server (optional dashboard)
HOST=127.0.0.1
PORT=8765
